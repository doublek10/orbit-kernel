# AI

AI never performs business actions - it only analyzes, predicts, scores,
and recommends. The Workflow Engine decides whether a recommendation
becomes an action.

Not implemented yet. Planned shape: a thin client to whichever model
provider is chosen, called only from within a Workflow, never directly
from the Gateway or Frontend.
