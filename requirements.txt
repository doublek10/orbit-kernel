fastapi==0.115.0
uvicorn[standard]==0.30.6
asyncpg==0.29.0
PyJWT==2.9.0
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
email-validator==2.2.0
pytest==8.3.3
pytest-asyncio==0.24.0
cryptography==43.0.1

# Optional - only needed for a live Test Connection in the Connector
# Generator (kernel/company_blueprint/connector_tester.py). Postgres
# already works via asyncpg above. Without these, connector.test for
# the matching database honestly reports "driver not installed"
# instead of failing the whole Kernel.
aiomysql==0.2.0      # mysql
motor==3.6.0         # mongodb
aiosqlite==0.20.0    # sqlite
pymssql==2.3.2       # sqlserver
